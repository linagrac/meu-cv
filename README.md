# Personal CV

## Requirements

1. Docker
2. Docker Compose

> **IMPORTANT!**   
> Don't try to install LaTex locally, just use Docker, trust me

## Creating CV, Resume, CoverLetter

Run:
```shell
docker-compose run --rm latex resume
```

Grab your `pdf` in the `output` folder created at the root of the repository
